# Docker Compose для Canary Deployment
# Разворачивает v1 и v2 user-service с балансировкой 90% / 10%

networks:
  backend:
    external: true
    name: pg_auto_default
  frontend:
    driver: bridge

services:
  # =====================================================
  # ValKey (Redis-compatible cache)
  # =====================================================
  valkey:
    image: valkey/valkey:7-alpine
    container_name: search-valkey
    ports:
      - "6379:6379"
    networks:
      - backend
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # =====================================================
  # RabbitMQ
  # =====================================================
  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: search-rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    networks:
      - backend
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # =====================================================
  # User Service V1 (90% трафика)
  # =====================================================
  user-service-v1:
    build:
      context: ./services/user-service
      dockerfile: Dockerfile
    container_name: search-user-service-v1
    environment:
      DATABASE_URL: postgresql://docker:secret@pgauto-node2:5432/users_db
      REDIS_URL: redis://valkey:6379/0
      SECRET_KEY: your-secret-key-change-in-production
      AUDIT_SERVICE_URL: http://audit-service:8000
    networks:
      - backend
    depends_on:
      valkey:
        condition: service_healthy

  # =====================================================
  # User Service V2 (10% трафика) - НОВАЯ ВЕРСИЯ 🆕
  # =====================================================
  user-service-v2:
    build:
      context: ./services/user-service-v2
      dockerfile: Dockerfile
    container_name: search-user-service-v2
    environment:
      DATABASE_URL: postgresql://docker:secret@pgauto-node2:5432/users_db
      REDIS_URL: redis://valkey:6379/0
      SECRET_KEY: your-secret-key-change-in-production
      AUDIT_SERVICE_URL: http://audit-service:8000
    networks:
      - backend
    depends_on:
      valkey:
        condition: service_healthy

  # =====================================================
  # Document Service
  # =====================================================
  document-service:
    build:
      context: ./services/document-service
      dockerfile: Dockerfile
    container_name: search-document-service
    environment:
      DATABASE_URL: postgresql://docker:secret@pgauto-node2:5432/documents_db
      RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672/
      AUDIT_SERVICE_URL: http://audit-service:8000
    networks:
      - backend
    depends_on:
      rabbitmq:
        condition: service_healthy

  # =====================================================
  # Search Service
  # =====================================================
  search-service:
    build:
      context: ./services/search-service
      dockerfile: Dockerfile
    container_name: search-search-service
    environment:
      DATABASE_URL_READ: postgresql://docker:secret@pgauto-node2:5432/search_db
      DATABASE_URL_WRITE: postgresql://docker:secret@pgauto-node2:5432/search_db
      REDIS_URL: redis://valkey:6379/1
    networks:
      - backend
    depends_on:
      valkey:
        condition: service_healthy

  # =====================================================
  # Indexer Worker
  # =====================================================
  indexer-worker:
    build:
      context: ./services/indexer-worker
      dockerfile: Dockerfile
    container_name: search-indexer-worker
    environment:
      DATABASE_URL: postgresql://docker:secret@pgauto-node2:5432/search_db
      RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672/
    networks:
      - backend
    depends_on:
      rabbitmq:
        condition: service_healthy

  # =====================================================
  # Audit Service
  # =====================================================
  audit-service:
    build:
      context: ./services/audit-service
      dockerfile: Dockerfile
    container_name: search-audit-service
    environment:
      DATABASE_URL: postgresql://docker:secret@pgauto-node2:5432/audit_db
    networks:
      - backend

  # =====================================================
  # Nginx Gateway (Canary с split_clients)
  # =====================================================
  nginx:
    image: nginx:alpine
    container_name: search-nginx-canary
    ports:
      - "80:80"
    volumes:
      - ./services/gateway/static:/usr/share/nginx/html:ro
      - ./services/gateway/nginx.canary.conf:/etc/nginx/nginx.conf:ro
    networks:
      - frontend
      - backend
    depends_on:
      - user-service-v1
      - user-service-v2
      - document-service
      - search-service

  # =====================================================
  # Monitoring
  # =====================================================
  prometheus:
    image: prom/prometheus:latest
    container_name: search-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    networks:
      - backend
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'

  grafana:
    image: grafana/grafana:latest
    container_name: search-grafana
    ports:
      - "3000:3000"
    environment:
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_SERVER_ROOT_URL: http://localhost:3000
    volumes:
      - grafana-data:/var/lib/grafana
    networks:
      - backend
    depends_on:
      - prometheus

volumes:
  grafana-data:
